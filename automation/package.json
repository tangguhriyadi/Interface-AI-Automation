{
  "name": "automation",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Computer-use automation system core: capability/app-profile/tenant-overlay schemas, the Playwright surface adapter, and the deterministic replay executor.",
  "scripts": {
    "build": "tsc --noEmit",
    "test": "vitest run --exclude \"test/integration/**\"",
    "test:integration": "vitest run test/integration"
  },
  "dependencies": {
    "playwright": "^1.47.0",
    "yaml": "^2.5.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "tsx": "^4.16.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
