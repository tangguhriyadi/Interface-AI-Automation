# Captured via `page.locator("body").ariaSnapshot()` against a live target-app
# instance (search -> memberId "abcd" -> GET /members/abcd, main frame), 2026-09-23.
- table:
  - rowgroup:
    - row "Invalid Input":
      - cell "Invalid Input":
        - heading "Invalid Input" [level=1]
    - row:
      - cell:
        - alert: Member ID must contain only digits.
